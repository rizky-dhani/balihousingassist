## ADDED Requirements

### Requirement: Inter font is globally applied
This requirement MUST ensure that the Inter font is set as the primary sans-serif font throughout the application.

#### Scenario: Application loads
Given the application loads any page,
When the page is rendered,
Then the primary sans-serif font used for text MUST be Inter.
