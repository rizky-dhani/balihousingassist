# Use Inter Font as Global Font

This proposal investigates the use of Inter font as the global font for the application. Upon investigation, it was found that Inter font is already configured and applied globally.
